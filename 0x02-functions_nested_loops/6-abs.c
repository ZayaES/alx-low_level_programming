/* #include "main.h" */

/**
 * _abs - Checks if input is posirive, zero or negative
 * @n: First operand
 *
 * Return: 1 if positive, 0 if zero, -1 if negative
*/

int _abs(int n)
{
#include "main.h"

	if(n >= 0)
	{
		return (n);
	}
	else
	{
		return (-1*n);
	}
}
