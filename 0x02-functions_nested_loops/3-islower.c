/* #include "main.h" */

/**
 * _islower - Checks if input is lowercase
 *
 * Return: 1 if lowercase, 0 else
*/

int _islower(int c)
{
#include "main.h"
{
	if ((c >= 97) && (c <= 122))
	{
		return (1);
	}
	else 
	{
		return (0);
	}
}
